---
title: "The Stand"
type: book
status: in-progress
date: 2026-06-28
description: >-
  Currently listening to the uncut audiobook.
---

<!-- `status: in-progress` is what makes this appear on `/now/` — no
separate now-page editing needed. Flip it to `status: finished` (and
update `date` to when you actually finished, plus add a `rating` if
you want one) once you're done, and it moves into the regular
reverse-chron feed on `/media/` instead, and drops off `/now/`. -->
