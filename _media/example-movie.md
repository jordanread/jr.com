---
title: "Example Movie — delete or repurpose me"
type: movie
date: 2026-06-20
rating: 4
description: >-
  One or two sentences — shown on the /media/ feed card and at the
  top of this entry's own page.
published: false
---

Optional longer write-up goes here as normal Markdown. Leave the
body empty and only the `description` blurb shows — no empty
content section gets rendered on the entry page either way.

No `status` field means this counts as finished by default (no
status badge shown). Set `status: in-progress` while you're partway
through something instead — see the two other example entries in
this folder — and it'll show up on `/now/` automatically.
