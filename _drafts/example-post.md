---
title: "Example Post — delete or repurpose me"
description: >-
  Optional one-liner shown on the blog index card and used as the meta
  description on the post page. Leave it off and the post's own excerpt
  gets used instead.
tags: [meta]
---

This lives in `_drafts/`, so it won't show up in `site.posts` or get built
until you either move it into `_posts/` with a `YYYY-MM-DD-slug.md`
filename, or run the server with `jekyll serve --drafts` to preview it.

Everything above the `<!--more-->` marker below is what shows as the
excerpt on the `/blog/` listing page if you don't set `description` in
the front matter.

<!--more-->

The rest of the post goes here. Standard Kramdown/Markdown — headings,
`code`, lists, links, images, all work as usual.
